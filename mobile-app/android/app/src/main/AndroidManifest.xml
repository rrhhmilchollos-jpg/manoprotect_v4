<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- ========================================== -->
    <!-- PERMISOS PARA SISTEMA DE ALERTA CRÍTICA SOS -->
    <!-- ========================================== -->
    
    <!-- Internet para comunicación con servidor -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    
    <!-- UBICACIÓN: Requerido para tracking durante emergencia -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <!-- CRÍTICO: Ubicación en segundo plano para tracking continuo durante SOS -->
    <uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION" />
    
    <!-- VIBRACIÓN: Para alertas de emergencia -->
    <uses-permission android:name="android.permission.VIBRATE" />
    
    <!-- SERVICIOS EN PRIMER PLANO: Para mantener el servicio SOS activo -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_LOCATION" />
    
    <!-- WAKE LOCK: Para mantener el dispositivo despierto durante emergencia -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    
    <!-- PANTALLA DE BLOQUEO: Para mostrar alerta sobre lock screen -->
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    <uses-permission android:name="android.permission.USE_FULL_SCREEN_INTENT" />
    <uses-permission android:name="android.permission.DISABLE_KEYGUARD" />
    
    <!-- NOTIFICACIONES: Para push notifications de emergencia -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    
    <!-- AUDIO: Para controlar volumen de sirena -->
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />
    
    <!-- POLÍTICAS DE DISPOSITIVO: Para bypass de Do Not Disturb -->
    <uses-permission android:name="android.permission.ACCESS_NOTIFICATION_POLICY" />
    
    <!-- CONTACTOS: Para selección de contactos de emergencia -->
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    
    <!-- TELÉFONO: Para llamar al 112 -->
    <uses-permission android:name="android.permission.CALL_PHONE" />
    
    <!-- BIOMETRÍA: Para autenticación segura -->
    <uses-permission android:name="android.permission.USE_BIOMETRIC" />
    <uses-permission android:name="android.permission.USE_FINGERPRINT" />
    
    <!-- CÁMARA: Para escanear códigos QR -->
    <uses-permission android:name="android.permission.CAMERA" />
    
    <uses-feature android:name="android.hardware.camera" android:required="false" />
    <uses-feature android:name="android.hardware.camera.autofocus" android:required="false" />
    <uses-feature android:name="android.hardware.location.gps" android:required="true" />

    <application
        android:name=".MainApplication"
        android:label="@string/app_name"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:allowBackup="false"
        android:theme="@style/AppTheme"
        android:supportsRtl="true"
        android:networkSecurityConfig="@xml/network_security_config"
        tools:targetApi="34">
        
        <!-- ========================================== -->
        <!-- ACTIVIDAD PRINCIPAL -->
        <!-- ========================================== -->
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:configChanges="keyboard|keyboardHidden|orientation|screenLayout|screenSize|smallestScreenSize|uiMode"
            android:launchMode="singleTask"
            android:windowSoftInputMode="adjustResize"
            android:exported="true"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <!-- Deep linking -->
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="mano" />
                <data android:scheme="manoprotect" />
                <data android:scheme="https" android:host="manoprotect.com" />
            </intent-filter>
        </activity>
        
        <!-- ========================================== -->
        <!-- ACTIVIDAD DE PANTALLA DE BLOQUEO SOS -->
        <!-- ========================================== -->
        <activity
            android:name="com.manoprotect.sos.SOSLockScreenActivity"
            android:label="Emergencia SOS"
            android:theme="@style/AppTheme.Fullscreen"
            android:launchMode="singleTop"
            android:excludeFromRecents="true"
            android:taskAffinity=""
            android:showOnLockScreen="true"
            android:turnScreenOn="true"
            android:showWhenLocked="true"
            android:exported="false">
        </activity>
        
        <!-- ========================================== -->
        <!-- SERVICIO DE ALERTA CRÍTICA SOS -->
        <!-- ========================================== -->
        <service
            android:name="com.manoprotect.sos.SOSCriticalAlertService"
            android:enabled="true"
            android:exported="false"
            android:foregroundServiceType="location">
        </service>
        
        <!-- ========================================== -->
        <!-- SERVICIO DE FIREBASE CLOUD MESSAGING -->
        <!-- ========================================== -->
        <service
            android:name="com.manoprotect.sos.SOSFirebaseMessagingService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>
        
        <!-- Canal de notificación por defecto para FCM -->
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_channel_id"
            android:value="sos_emergency_critical" />
        
        <!-- Icono de notificación por defecto -->
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_icon"
            android:resource="@drawable/ic_notification" />
        
        <!-- Color de notificación por defecto (rojo para emergencia) -->
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_color"
            android:resource="@color/notification_red" />
        
        <!-- ========================================== -->
        <!-- RECEIVER PARA BOOT COMPLETED -->
        <!-- ========================================== -->
        <receiver
            android:name="com.manoprotect.sos.BootCompletedReceiver"
            android:enabled="true"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.QUICKBOOT_POWERON" />
            </intent-filter>
        </receiver>
        
        <!-- Google Play Services -->
        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version" />

    </application>

</manifest>
